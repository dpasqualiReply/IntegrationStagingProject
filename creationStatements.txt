CREATE TABLE datamart.genometags (   tagid INT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   tag STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   PRIMARY KEY (tagid) ) PARTITION BY HASH (tagid) PARTITIONS 2 STORED AS KUDU TBLPROPERTIES ('kudu.master_addresses'='cloudera-vm.c.endless-upgrade-187216.internal')

CREATE TABLE datamart_test.movies (   movieid INT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   title STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   genres STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   link STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   PRIMARY KEY (movieid) ) PARTITION BY HASH (movieid) PARTITIONS 2 STORED AS KUDU TBLPROPERTIES ('kudu.master_addresses'='cloudera-vm.c.endless-upgrade-187216.internal')

CREATE TABLE datamart_test.ratings (   userid BIGINT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   movieid BIGINT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   rating DOUBLE NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   time STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   PRIMARY KEY (userid, movieid) ) PARTITION BY HASH (userid) PARTITIONS 2, HASH (movieid) PARTITIONS 2 STORED AS KUDU TBLPROPERTIES ('kudu.master_addresses'='cloudera-vm.c.endless-upgrade-187216.internal')

CREATE TABLE datamart_test.tags (   userid BIGINT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   movieid BIGINT NOT NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   tag STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   time STRING NULL ENCODING AUTO_ENCODING COMPRESSION DEFAULT_COMPRESSION,   PRIMARY KEY (userid, movieid) ) PARTITION BY HASH (userid) PARTITIONS 2, HASH (movieid) PARTITIONS 2 STORED AS KUDU TBLPROPERTIES ('kudu.master_addresses'='cloudera-vm.c.endless-upgrade-187216.internal')

---

